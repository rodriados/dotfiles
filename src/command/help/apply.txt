Usage: dotfiles apply [options] [file ...]

Install dotfiles from recipe files.

Arguments:
  file             Recipe file for dotfiles installation

Options:
  --dry-run, -d    Run in dry-run mode and makes no changes
  --help, -h       Display this help message and exit
